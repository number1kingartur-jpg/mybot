/** Посты @kingmode_fit — только практика в голосе Артура. Без цен и воронки. */

export interface ChannelPost {
  id: string;
  title: string;
  body: string;
}

function post(title: string, body: string, id: string): ChannelPost {
  return { id, title, body };
}

/**
 * Автопост (пн/ср/пт) — одна мысль, твой тон (#8–9).
 * Форматы, цены, книга — только вручную или кнопки под постом.
 */
export const CHANNEL_POSTS: ChannelPost[] = [
  post(
    "Данные вместо ощущений",
    `Раньше я тоже верил ощущениям.\n\n` +
    `«Мало ем» — а на выходных съедал лишнее.\n` +
    `«Тренируюсь нормально» — а объём за месяц упал.\n\n` +
    `По факту решения проще через цифры: веса, повторения, вес тела.\n\n` +
    `Не усложняй — начни записывать.`,
    "data"
  ),
  post(
    "Сон",
    `Три ночи по 5 часов — и я всё равно шёл в зал как герой.\n\n` +
    `По факту сон — не «остаток дня». Это часть тренировки.\n\n` +
    `Меньше сил. Дольше восстановление. Хуже техника.\n\n` +
    `Иногда лучшее решение — не добивать, а выспаться.`,
    "sleep"
  ),
  post(
    "Плато",
    `Упёрся в плато — сначала не меняю программу.\n\n` +
    `Проверяю базу:\n` +
    `— сон\n` +
    `— еда под цель\n` +
    `— объём за месяц\n\n` +
    `Часто проблема не в схеме. А в том, что база поплыла.\n\n` +
    `Одна плохая неделя — не повод менять всю жизнь.`,
    "plateau"
  ),
];
