/**
 * Волна restart: прогрев под «Старт 3 дня». Аудитория: снова начинающие.
 */
import type { ChannelPost } from "./posts";

function r(title: string, body: string, id: string, guideStart?: string): ChannelPost {
  return { id, title, body, guideStart };
}

const RESTART_PHOTOS: Record<string, string> = {
  restart_skip: "generated/akf-2026-08/sleep-bg-5-clock-night.png",
  restart_brain: "generated/akf-2026-08/prog-bg-8-notebook.png",
  restart_begin: "generated/akf-2026-08/sleep-bg-1-cover.png",
};

const RESTART_RAW: ChannelPost[] = [
  r(
    "Один пропуск",
    `Пропустил один день и решил, что все, конец. Знакомо?\n\n` +
      `Так не работает ни в зале, ни в жизни. Один пропуск это просто день. Два подряд уже привычка, и она формируется так же быстро, как нужная.\n\n` +
      `Правило простое: не руби все под корень. Вернулся на следующий день и продолжил, как будто ничего не было.\n\n` +
      `Именно поэтому я собрал короткий старт в боте. Три дня, по пятнадцать минут дома. Без зала и без «с понедельника».`,
    "restart_skip"
  ),

  r(
    "Мозг против",
    `Когда ты решаешь что-то менять, внутри включается сопротивление. Мозг не орет «не делай». Он подсовывает разумную отговорку, и ты соглашаешься.\n\n` +
      `«Сегодня некогда». «Завтра легче». «Сначала доделаю дела». Звучит логично. Поэтому и опасно.\n\n` +
      `Он воюет только с большим рывком. Маленький шаг он часто пропускает. Пятнадцать минут дома, вода, одно движение. Этого мало, чтобы включилась паника.\n\n` +
      `Три таких дня подряд. Не марафон. Проверка, что ты можешь держать слово перед собой.`,
    "restart_brain"
  ),

  r(
    "Старт 3 дня",
    `Если ты давно откладываешь, вот формат без героизма.\n\n` +
      `Три дня. Каждый день:\n` +
      `1. короткое движение дома, около пятнадцати минут;\n` +
      `2. одна простая задача: вода, вес или белок в одном приеме;\n` +
      `3. кнопка «сделал» в боте, чтобы не врать себе.\n\n` +
      `Без зала. Без формы. Без списка «нельзя». Задача не похудеть за три дня, а доказать, что можешь не слиться на старте.\n\n` +
      `Дальше, если зайдет, есть полный цикл «30 дней шаг за шагом». Сначала пройди три дня бесплатно.`,
    "restart_begin",
    "restart_3d"
  ),
];

export const CHANNEL_POSTS_RESTART: ChannelPost[] = RESTART_RAW.map((p) => {
  const archiveImage = RESTART_PHOTOS[p.id];
  if (!archiveImage) throw new Error(`restart: нет фото для ${p.id}`);
  return { ...p, archiveImage };
});
